// API Configuration
// Fallback URL for when environment variable is not set during build
export const BACKEND_URL = process.env.EXPO_PUBLIC_BACKEND_URL || 'https://foodcoach-android.preview.emergentagent.com';
